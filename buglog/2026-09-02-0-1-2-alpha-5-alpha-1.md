---
date: "2026-09-02T16:47:39.605Z"
symptom: "官方仓库更新到 0.1.2-alpha.5 后版本台账仍停在 alpha.1、看门狗为旧实例"
component: "DSH-ops update-dsh.ps1 / version-history.md"
severity: "minor"
status: "workaround"
root_cause: "用户手动拉取+构建+重启，未走 update-dsh.ps1 完整升级链，台账未自动记录、看门狗未随新启动链轮换。"
fix: "核查确认无破坏性变更；建议补记版本台账（alpha.5，49a606bc5b）并择机用 update-dsh.ps1 完整链重走一遍让台账/看门狗/启动链三处同步。"
related_files:
---

用户手动拉取 master（HEAD 49a606bc5b = 0.1.2-alpha.5）并自行构建重启（新服务 41840 于 09-03 00:37:20，lib 产物 00:36:59 对应新 HEAD）。核查结果：① 9 个自研插件在新构建下闸门全 PASS、切 reload 无 err.log；② 服务 41840 健康、HEALTH 全绿 exit 0；③ 关键 API 面核查：session-controller 仅新增 coldBlankProbeMaxEvents（非破坏）、shell-env 移除 DSH_SESSION_JSONL 变量（ctx.shellEnv 服务本体不变）、slots 仅新增 tool.call.images 与 chat.node owner 新增 loadImage（均为新增）、conversation.session.header.utilities 未变；④ SESSION_FORMAT_VERSION 保持 0；⑤ projcache 域名升 v6 但官方声明 compatibleVersions:[3,4,5] + backup-and-skip，跨版本读取安全（2026-09-02-projcache-cross-version-read-compat note 全文）。遗留：version-history.md 台账未更新（停在 alpha.1/8-28，实际已到 alpha.5）；看门狗 29900 仍是昨晚实例（服务重启未走新启动链，但功能正常）。建议：台账手动补记 alpha.5。
